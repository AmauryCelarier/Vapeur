<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un éditeurs</title>
    <link rel="stylesheet" href="/css/main.css"> <!-- Lien vers le fichier CSS -->
</head>
<body>
{{> header}}
<div class="container">
<h1>Jeux du genre : {{genre.genre}}</h1>

{{#if games.length}}
    <ul>
        {{#each games}}
            <li><a href="/games/{{this.id_game}}">{{this.title}}</a> - {{this.editor.name}}</li>
        {{/each}}
    </ul>
{{else}}
    <p>Aucun jeu disponible pour ce genre.</p>
{{/if}}
</div>
{{> footer}}
</body>
</html>